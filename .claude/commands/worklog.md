---
description: Append a dated entry to docs/WORKLOG.md describing recent changes
---

Append a new entry to `docs/WORKLOG.md` for the work described below (or, if nothing
is specified, for the changes made so far in this session).

Rules:
- Prepend the entry directly under the `## Log` heading (newest first).
- Use the format: `### YYYY-MM-DD HH:MM — <short title>` followed by bullet points.
- Each bullet names a concrete change: file(s) touched, command run, decision made.
- If the change reflects a design decision, also add/update an entry in `docs/DECISIONS.md`.
- If the change is a hackathon iteration (new skill, verification step, orchestration
  change, measured result), also add a row to `docs/CHANGELOG.md`.
- Keep it factual. No marketing language.

Context / what to log: $ARGUMENTS
