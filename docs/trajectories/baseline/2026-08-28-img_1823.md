# Baseline trajectory — frame `img_1823`

> **Archived run.** From the `claude-sonnet-5` baseline on the earlier lightly-blurred
> frames; that cache was replaced on 2026-08-28 by a `claude-haiku-4-5` run on the committed
> frames (`docs/CHANGELOG.md`). Preserved at commit `e8de845`; `--replay` from that checkout
> reproduces it. The failure it shows — a broken machine confidently called `free` — still
> holds on haiku (which calls it `occupied` instead; `out_of_order` recall stays 0/8).

The baseline (`runBaseline`, `src/agent/baseline.ts`): **one** `claude-sonnet-5` vision call
on the whole frame, told the machine-id list + numbering convention, no ROI / verification /
memory. Served from `data/cache/baseline/` at commit `e8de845`.

**Frame:** wide view of the dryer wall — D-01/D-02 … D-09/D-10 in five stacked columns,
plus W-16 (a top-load washer) in the foreground. Privacy redaction blurs the sticker band.

**Ground truth** (`data/labels/img_1823.json`): D-03 free, D-04 free, D-05 occupied,
D-06 **out_of_order**, D-07 occupied, D-09 free, W-16 free (determinate); D-01, D-02, D-08,
D-10 `unknown` (author couldn't tell).

## The one call

Prompt: `baselinePrompt([D-01…D-10, W-16])`. Model reply (verbatim, trimmed to determinate
machines):

| id | model | conf | rationale | GT | verdict |
| --- | --- | --- | --- | --- | --- |
| D-03 | free | 0.5 | "Top dryer door, no info visible" | free | ✓ |
| D-04 | occupied | 0.7 | "display showing 225 time remaining" | free | ✗ |
| D-05 | free | 0.5 | "display not clearly showing active run" | occupied | ✗ **harmful** |
| D-06 | free | 0.5 | "Bottom dryer, display not clearly showing active run" | out_of_order | ✗ **harmful** |
| D-07 | free | 0.5 | "Top dryer door, no info visible" | occupied | ✗ **harmful** |
| D-09 | free | 0.4 | "Top dryer door, no info visible" | free | ✓ |
| W-16 | free | 0.5 | "drum empty and door open" | free | ✓ |

3 / 7 correct on this frame; **3 harmful errors** — including calling **D-06 "free" when its
display reads `E rot`** (a hard error). That is exactly the wasted-trip failure the product
exists to prevent.

## What this shows

The baseline's default behaviour is "everything is `free` at ~0.5 confidence". With no
per-machine look it can't read the small displays through the redaction blur, so it neither
abstains nor flags the broken machine — it guesses `free`. `225` (a *price*, $2.25) is also
misread as "time remaining", producing false `occupied`. Iteration 1's verification pass
targets exactly these: re-look at the low-confidence machines, name the out-of-order cues,
and abstain rather than guess (`docs/trajectories/runtime/2026-08-28-img_1825.md`).

1 vision call, ~$0.011, ~2.9k in / 0.5k out tokens.
