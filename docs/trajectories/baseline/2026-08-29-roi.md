# ROI-agent trajectory — per-machine crops from a colour-coded region map

- **Agent:** `runRoi` (`src/agent/roi.ts`), with `loadMaskRegions` + `groupRegions`
  (`src/eval/mask-regions.ts`).
- **Instructions file:** the `RULES` array + `soloPrompt` / `stackPrompt` in
  `src/agent/roi.ts` (frozen — the "left/right" variant).
- **Trigger:** `npm run eval -- --mode=roi --split=evaluation --live` (2026-08-29). Cache
  keyed by crop bbox, not image bytes, so `--replay` reproduces without `sharp` cropping the
  frame.
- **Model:** `claude-haiku-4-5`. Served from `data/cache/roi/`; report
  `docs/artifacts/eval-roi-2026-08-29.json`.

## What the agent does, per frame

1. `loadMaskRegions(camera.mask, camera.maskLegend)` — read the region-map PNG, assign every
   non-black pixel to its nearest legend colour (Euclidean RGB, tolerance 120), union
   same-colour pixels into one bbox + centroid per machine id. **No positional inference:**
   the id follows the painted colour, so a camera at an angle or a non-standard stack is
   handled without guessing "leftmost = D-01".
2. `groupRegions` — merge a *vertically* stacked, x-overlapping pair (`dy > 1.6·dx`,
   x-overlap ≥ 0.45) into one shared-panel crop; keep a perspective-diagonal row as separate
   crops.
3. For each group: crop the live frame to the union bbox (pad 6 %; solo groups shrink 0.22
   toward centre), one `vision.analyze` call — `soloPrompt(id)` for a single machine,
   `stackPrompt(idUpper, idLower)` for a pair (tells the model the upper unit's readout is
   the LEFT one with the up-arrow, the lower unit's is the RIGHT one with the down-arrow).
4. `scoreAll(predictions, labels, frameIds, scope)` — scored only on the machines the camera
   is responsible for (`camera.machineIds`), same scope as the baseline.

## Result — 16 calls, `--replay`-reproducible

| Frame | Camera | scope | ROI verdicts vs GT | frame score |
| --- | --- | --- | --- | --- |
| img_1819 | C-01 | W-01–W-04 | W-01 free ✓ · W-02 free ✓ · W-03 free ✓ · W-04 occupied ✗ (GT out_of_order) | **3 / 4** |
| img_1821 | C-02 | D-01–D-07 | D-01 occupied ✓ · D-02 occupied ✗ (oo) · D-03/04 occupied ✗ (free) · D-05/06/07 unknown ✗ (grey redaction + backlit blur) | **1 / 7** |
| img_1822 | C-03 | D-01–D-02 | D-01 out_of_order ✗ (GT occupied) · D-02 occupied ✗ (GT out_of_order) — the two readings swapped | **0 / 2** |
| img_1823 | C-04 | D-03–D-10 | D-03/04 free ✓ · D-05 out_of_order ✗ (occ) · D-06 free ✗ (oo) · D-07 occupied ✓ · D-09 occupied ✗ (free) | **3 / 6** |
| img_8633 | C-05 | W-14–W-16 | W-14 free ✗ (GT occupied) · W-15 free ✓ · W-16 free ✓ | **2 / 3** |

**Totals: 9 / 22 = 40.9 %** · harmful-error 9.1 % · coverage 86.4 % · `out_of_order` recall
0 / 5 · ~$0.022. `--replay --corrections` → 63.6 %.

## Feedback that shaped the config — three prompt variants

The crop mechanism was fixed; the per-call prompt was iterated `--live`. **These per-variant
runs were not retained** (`docs/artifacts/eval-roi-samples-2026-08-29.md`) — the accuracy
figures are session notes, not reproducible artifacts; only the frozen config's 3 samples are:

| Variant | What it told the model | Accuracy | Why not kept |
| --- | --- | --- | --- |
| vague up/down | "read the upper / lower readout" | 40.9 % | ambiguous on the stacked panels |
| **explicit left/right** (frozen) | upper unit = LEFT readout (↑), lower unit = RIGHT readout (↓) | 40.9 % (re-ran; 50.0 % on a first sample) | **this is the committed config** |
| + worn-segment guidance | "a dim / missing segment — read the shape, don't over-read" | 54.5 % | the higher number came from the model guessing more, which also raised harmful-error; and `out_of_order` is the corrections layer's job, not ROI's — disclosed and left out |

The frozen config re-ran to **40.9 % on all 3 recorded samples**
(`docs/artifacts/eval-roi-samples-2026-08-29.md`); harmful-error and coverage moved
4.5–13.6 % / 82–86 % across those three runs.

## Outcome

- **Kept as an iteration, not a measured win.** The architecture is right — layout-independent
  per-machine crops, id from paint not position — but as frozen it is a consistent **~4.6 pp
  below the 45.5 % baseline**.
- The mechanism visibly helps where the display is readable: C-01 (front-on) 3 / 4, and
  `free` precision improves (tight crop + the "`2.25` is a price, not a countdown" rule cuts
  the free→occupied over-call from 6 / 10 to 3 / 10).
- It fails where `claude-haiku-4-5` cannot OCR small / worn / backlit 7-segment displays in a
  wide angled shot — C-02 1 / 7, C-03 0 / 2 (the two readings literally swapped).
- `out_of_order` stays 0 / 5: cropping does not help the model tell `E rot` from a running
  cycle. That failure is cleared only by **integrator corrections** (D-0014), which remain the
  improvement path. Kept in-tree (`--mode=roi`, cache committed) so the neutral result
  reproduces.
