# Compliance review — 2026-08-29 — `--mode=roi` (per-machine crops from a region map)

**Change under review:** region-map masks (5 `img_*.mask.png` repainted colour-coded) +
`camera.maskLegend`, `src/eval/mask-regions.ts`, `src/agent/roi.ts`, `--mode=roi` eval path,
`docs/artifacts/eval-roi{,-corrected}-2026-08-29.json` + `data/cache/roi/`, tests, and docs.

## Verdict: PASS WITH RISKS

### Blockers (eligibility)

None.

- **G2** — provenance section intact.
- **G4/G5** — ROI is an offline classification/eval path, no real-world side effects; the
  integrator corrections keep a human in the loop.
- **G7** — the 5 `img_*.mask.png` are same-name allow-listed files with content replaced by
  derived solid-colour maps; no new data.
- **G8** — secret scan of the full staged diff clean (only token *counts* in the artifacts).
- **G9** — the headline numbers (40.9% / 9.1% / 86.4% / 0/5 / 63.6%) reconcile to the
  committed reports and `data/cache/roi/` (16 files).

### Risks and how they were handled

1. **Evidence coverage.** The "repeated runs span ~41–55% / indistinguishable from baseline"
   claim had only the single 40.9% report committed, and it was load-bearing.
   → **Handled — and it was an over-claim.** Ran the *frozen* config two more times:
   accuracy was **stable at 40.9% on all 3 samples** (harmful-error 4.5–13.6%, coverage
   82–86% vary). The 41–55% spread was across the *prompt variants* tried during
   development, not run-to-run noise on the committed config. Committed
   `docs/artifacts/eval-roi-samples-2026-08-29.md` with the 3 runs and rewrote README /
   CHANGELOG / EVALUATION / REPRODUCTION to "40.9%, a consistent ~4.6 pp below baseline"
   (dropping "within noise" / "statistically indistinguishable"). The retained
   `data/cache/roi/` is run 1, matching the committed report (verified byte-for-byte after
   restoring it).

2. **`sharp` on the `--replay` path.** `sharp` was only an *optional* transitive dep of
   `next`; `--mode=roi --replay` imports it to derive bboxes, so `npm ci --no-optional` or a
   platform without a prebuild would break ROI replay.
   → **Handled.** Added `sharp` as an explicit `devDependency` pinned to `^0.35.3` (the range
   `next` uses; resolves to the same `0.35.4` already in the lockfile — no version churn),
   `package-lock.json` updated, and `docs/REPRODUCTION.md` notes `npm ci` installs it.

3. **Process.** WORKLOG compliance line was "pending"; no trajectory file.
   → **Handled.** This file; WORKLOG line updated with the verdict and risk handling.

### Notes (from the review)

- Prompt-variant selection is conservative, not tuning-to-test: the frozen config is the
  middle "left/right" variant, the recorded run is the lowest of the three, and the
  factually-correct worn-segment guidance was excluded with a stated rationale.
- Keeping a non-winning iteration in-tree is what the Improvement Changelog rule asks for;
  the framing is consistent with the `--mode=calibrated` dead-end and the D-0014
  verification-pass precedents.
- Owner-authored `maskLegend` colours / region shapes are a resource asymmetry vs the
  baseline (scoped id list only) — disclosed in the ROI description; the A/B still scores the
  same 22 camera-scoped observations.
