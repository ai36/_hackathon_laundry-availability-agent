# Compliance review — agent Iteration 1 + cost controls (3 rounds)

- **Agent:** hackathon-compliance (`.claude/agents/hackathon-compliance.md`)
- **Trigger:** `runAgent` verification pass; committed `data/cache/agent/` + report;
  cost-control model switch; G9 fixes
- **Date:** 2026-08-28
- **Model:** sonnet

## Round 1 — CHANGES REQUIRED (G9)

CHANGELOG/WORKLOG claimed the deltas were "directionally consistent across 3 agent samples"
but only one sample is committed. Other risks: no agent results table in REPRODUCTION;
`npm test` expectation stale (10/10); the abstain-floor recalibration had no ADR;
`meta.needsVerification` held *verified* ids (misleading name).

## Round 2 — CHANGES REQUIRED (repeat)

Same G9 blocker still present. (The round-1 fixes had not yet been committed — this was the
first commit of the whole Iteration-1 change.)

## Round 3 — PASS WITH RISKS

Blocker cleared: the "3 samples" claim removed; now "one unaveraged sample per mode".
Independently verified: clean-checkout `--replay` (frames + `.env` moved aside, key unset)
reproduces baseline **31.1 / 11.1 / 60.0 / 51.9** and agent **31.1 / 8.9 / 51.1 / 60.9 /
oo 2/8** byte-identically; both reports carry `model: claude-sonnet-5` from the cache (config
default is now `claude-haiku-4-5`); G8 clean.

Round-3 risks + fixes applied:

- Dead-end "4.4%" figures had no artifact → softened to "~4% (dev observation, not
  archived)" in CHANGELOG (×2), WORKLOG, D-0013.
- `docs/EVALUATION.md` §Results had no agent entry → added the Iteration-1 results table
  mirroring the baseline.
- `docs/REPRODUCTION.md` stale bits → removed the sonnet-only `--live` dollar figures,
  corrected the "(model from `laundry3.config.ts`)" note to "read from the cache", added a
  `LAUNDRY3_VISION_MODEL=claude-sonnet-5` note for matching the recorded model.
- Added `docs/trajectories/runtime/2026-08-28-img_1825.md` — a representative
  classify → verify → override trajectory (deliverable #4).

Standing (disclosed) limitations: single sample per mode; 9 frames; multi-sample averaging
deferred for API budget.
