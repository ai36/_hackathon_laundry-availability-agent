# Runtime agent trajectory — frame `img_1825`

> **Archived run.** This trajectory is from the `claude-sonnet-5` agent run on the earlier
> lightly-blurred frames. That cache was replaced on 2026-08-28 by a `claude-haiku-4-5` run
> on the committed author-redacted frames (see `docs/CHANGELOG.md`); it is preserved in git
> at commit `e8de845` and reproduces via `--replay` from that checkout. Kept here because it
> is the clearest illustration of the verification pass *losing* a correct answer — a
> behaviour the haiku run shows differently (it over-commits rather than over-abstains). A
> fresh haiku trajectory is a TODO.

One representative run of `runAgent` (`src/agent/pipeline.ts`, Iteration 1). Model
`claude-sonnet-5`; served from `data/cache/agent/` at commit `e8de845`.

**Frame:** close-up of one stacked dryer column — D-04 (bottom-left, blurred), D-05 (top),
D-06 (Speed Queen, showing `40` / `oF`), D-08 (bottom-right). Privacy redaction blurs the
sticker band across the top.

**Ground truth** (`data/labels/img_1825.json`): D-04 `unknown` (author couldn't tell),
D-05 `occupied`, D-06 `out_of_order`, D-08 `unknown`.

---

## Step 1 — classify (whole frame, all 4 ids)

Prompt: `classifyPrompt(...)` — states + numbering convention + `Classify these machines: D-04, D-05, D-06, D-08`.

Model reply:

| id | state | conf | rationale |
| --- | --- | --- | --- |
| D-04 | unknown | 0.3 | Blurred/out of focus, no clear evidence |
| D-05 | occupied | 0.6 | Display shows time (40) suggesting active cycle |
| D-06 | unknown | 0.3 | Blurred/out of focus, no clear evidence |
| D-08 | unknown | 0.3 | Blurred/out of focus, no clear evidence |

All four are ≤ 0.6, below the `verification.confidenceThreshold` (0.7) or already `unknown`
→ **all four flagged for a second look.**

## Step 2 — verification pass (focused, same 4 ids)

Prompt: `verifyPrompt(...)` — "study each one's panel/display/door/drum; describe what you
see, then classify; out-of-order cues: `Err` / `E rot` / `oF` / dark panel / tape; dim
7-segment segments — read the shape, don't over-read".

Model reply:

| id | state | conf | rationale |
| --- | --- | --- | --- |
| D-04 | unknown | 0.3 | leftmost panel blurred/out of frame, cannot confirm identity or display |
| D-05 | unknown | 0.3 | blue card-reader screen but no machine ID visible to confirm this is D-05 |
| **D-06** | **out_of_order** | 0.55 | "shows `40` on left display and `oF` on right display, which reads as an error/off-state code rather than a normal countdown or price; door closed, no laundry visible" |
| D-08 | unknown | 0.3 | lit card-reader screen but no visible ID |

Verify answers override pass 1 for all four (`verifiedMachineIds: [D-04, D-05, D-06, D-08]`).

## Step 3 — abstain floor

Floor = 0.35. Only D-04/D-05/D-08 remain (0.3) and are already `unknown`; D-06 (0.55) is an
answered machine above the floor → kept. Final = the Step-2 table.

## Outcome vs ground truth (determinate only: D-05, D-06)

- **D-06 `out_of_order` — correct.** The verification pass turned a pass-1 `unknown` into the
  right call by naming the `oF` code as an error cue. This is the win the verify step exists
  for (baseline never gets `out_of_order`).
- **D-05 `unknown` — incorrect** (GT `occupied`). Pass 1 had it right at 0.6 (`display shows
  40 → active cycle`); the verify pass, anchored on "confirm the machine ID", downgraded it
  to `unknown` because no printed id is visible. A verification step that over-weights
  identity confirmation can *lose* a correct state read — a known failure mode of Iteration
  1, and part of why coverage drops.

2 vision calls, ~$0.017, ~5.7k in / 0.55k out tokens.
