# Backlog

Deferred work, newest first. Small UI-polish items live here until picked up; anything
that changes behaviour, deps, or evidence still goes through the normal
WORKLOG/DECISIONS/CHANGELOG + `hackathon-compliance` flow when done.

_Nothing queued._

## Done

- **2026-08-29 — Portal button consistency + no dev ids in UI copy.** Camera image `remove`
  → icon-only; camera / machine `remove` → icon + label; mark-wrong panel → state
  selection + `save` + icon `cancel`; stripped `D-00xx` / `StaticImageFrameSource` from
  user-facing strings. See WORKLOG 2026-08-29.
