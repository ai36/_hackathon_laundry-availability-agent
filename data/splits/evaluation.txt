# Evaluation set — one still per calibrated camera (D-0015). Each frame carries a `camera`
# field in its label; the camera's entry in data/site-config.json scopes it to the machines
# that camera is responsible for, plus its annotated shot + analysis mask.
img_1819
img_1821
img_1822
img_1823
img_8633
