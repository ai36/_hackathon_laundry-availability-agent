# Copy to `.env` (git-ignored) and fill in. Never commit `.env` or paste keys anywhere.
#
# Required for a live eval run (not needed with --replay):
ANTHROPIC_API_KEY=

# Optional — overrides the vision model from laundry3.config.ts for a run:
# LAUNDRY3_VISION_MODEL=claude-sonnet-5
